# S10 Web/TUI 연결

상태: PASS
연결: F25,F26 및 S03/S05/S07 UI
선행: S03,S04,S05,S07,S08,S09

## 수정 시작점과 소유 범위

web/src/pages/Explorer.tsx,Chat.tsx,Workspace.tsx,Team.tsx; web/src/lib; cmd/proxy/tui_commands.go; server file/session APIs

경로는 시작점이다. 실제 production caller와 기존 테스트를 rg로 추적한다. 공통 파일은 plan.md 순차 소유 규칙을 따른다.

## 하위 단계

- A. [PASS] 기존 디자인 지침 확인 후 프로젝트→작업→세션→현재 단계/권한 표시를 연결한다. 새 대시보드 대량 추가는 하지 않는다.

  - 제품·web·docs 범위에서 `DESIGN.md`/`design.md`는 발견되지 않았다. 기존 CSS 토큰과 Chat 헤더의 얇은 context strip을 재사용해 새 대시보드를 만들지 않았다.
  - Web `Chat.tsx`는 현재 project(workspace label), Workstream, durable session의 짧은 ID/revision/lifecycle, 선택된 Plan stage/status, 서버가 확인한 effective execution policy mode/revision을 한 줄의 접근 가능한 `nav`에 표시한다. session 로드 시 저장된 execution policy를 복원하고, 실행 중 `session` SSE의 정책으로 갱신한다. 정책이 없거나 잘못된 mode/revision이면 `unknown`/`checking…`으로 표시하며 CLI 요청값을 추정하지 않는다. 기존 Workstream/Plan/Stage 선택·승인·session binding은 그대로 유지한다.
  - TUI SESSION rail은 session ID/revision/turns, Workstream ID, Plan@revision, Stage, permission mode@revision을 표시한다. 저장·재개·동기화된 `Session.ExecutionPolicy`를 기본으로 사용하고 현재 run의 유효한 SSE 정책으로 교체한다. 누락/잘못된 정책은 `—`로 닫는다. CLI option으로 권한을 추정하지 않는다.
  - 주요 변경: `web/src/pages/Chat.tsx`, `internal/server/webdist/index.html` 및 새 embed asset, `cmd/proxy/tui.go`, `cmd/proxy/tui_view.go`, `cmd/proxy/tui_test.go`.
  - 검증: `$env:GOTOOLCHAIN = 'go1.26.1'; go test ./cmd/proxy -count=1` — PASS, exit 0 (28.121s). TUI context/unknown policy/rail rendering focused tests와 full cmd/proxy suite를 포함한다. `npm run lint` — PASS, exit 0. `npm run build` — PASS, exit 0 (Vite 8.0.3; 단일 JS chunk >500 kB 경고만 유지). `git diff --check` — PASS, exit 0.
  - NOT RUN: TUI interactive terminal smoke. Web refresh와 context strip은 E의 Rod E2E로 수행했고, 독립 사용자 흐름 판정은 G2에 남겼다.

- B. Explorer 파일 열기는 bounded read API 사용. 디렉터리 포함 canonical relative path, binary/too-large/error handling을 제공한다. 모델 호출 제거.

  - 초기 재현: `internal/server/server.go`의 기존 `/api/file` handler가 `os.ReadFile`로 전체 파일을 메모리에 올린 뒤 100KB에서 잘랐고, 구형 `web/src/pages/Explorer.tsx`는 `/api/agent` SSE에 "Read the file ..."을 보내 모델 호출로 파일을 읽었다. 원래 응답은 요청 문자열을 그대로 `path`에 되돌려 canonical 경로를 보장하지 않았다.
  - 구현: `internal/server/file_api.go`에 단일 읽기 한도(100,000 bytes), UTF-8/NUL probe, 확장자 기반 image/code 분류, canonical workspace-relative 변환을 두고 `handleReadFile`이 bounded preview를 반환하도록 바꿨다. 디렉터리는 `type: directory`와 bounded tree entries, binary/image/unsupported는 명시적 type, 초과 텍스트는 `type: too_large`·`truncated`·preview, missing은 404로 응답한다. `web/src/lib/files.ts`의 공통 응답 helper를 `App.tsx`와 `Explorer.tsx`가 사용하며, directory/binary/too-large/error를 표시하고 텍스트 계열만 편집한다. `internal/server/webdist`에는 새 Web bundle을 갱신했다.
  - 회귀: `internal/server/file_api_test.go`가 서로 다른 디렉터리의 동일 basename, `..` 정규화, 디렉터리 entries, binary, 100KB 초과 preview, missing 404와 모델 호출 0회를 production `handleReadFile` 경로로 확인한다. 기존 등록 workspace/traversal 검증도 함께 통과했다.
  - 검증: `$env:GOTOOLCHAIN = 'go1.26.1'; go test ./internal/server -run '^(TestReadFileUsesBoundedDirectResponseAndCanonicalPaths|TestFileHandlersUseRegisteredClientWorkspace)$' -count=1 -v` — PASS, exit 0. `go test ./internal/server -count=1` — PASS, exit 0 (20.360s). `go vet ./internal/server` — PASS, exit 0. `npm run lint` — PASS, exit 0. `npm run build` — PASS, exit 0 (Vite 8.0.3; 단일 JS chunk >500 kB 경고만 유지). `git diff --check` — PASS, exit 0.
  - NOT RUN: 독립 사용자 흐름 판정은 G2에서 수행한다. 디렉터리·대형 파일·binary viewer는 E의 인증된 Chromium 흐름에서 확인했다.

- C. 누적 run/turn diff와 변경 파일 목록, conflict-aware 선택 복구를 연결한다.

  - 초기 상태: Web `Chat.tsx`와 TUI `tui.go`가 마지막 diff 하나만 보존·표시했고, undo 엔진의 `/undo --list`/`/undo --select` 결과는 일반 text만 전달되어 선택 UI가 구조화된 충돌 상태를 알 수 없었다.
  - 구현: Agent RunLoop가 `undo_preview`/`undo_result` SSE에 bounded ID·path·status와 복원 결과를 내보낸다. Web `Chat.tsx`는 turn 번호별 diff를 최대 200개 누적하고 변경 파일 목록, 현재 turn 개수, checkpoint 상태를 표시하며 `restorable` 항목만 `/undo --select <id>` 버튼을 제공한다. 충돌·unavailable 항목은 복원 버튼 없이 상태만 표시한다. TUI는 한 run의 모든 diff와 unique 변경 파일을 `/diff`로 요약한다. CLI output normalizer도 새 구조화 event를 allowlist·sanitize한다.
  - 회귀: 기존 conflict-aware checkpoint fixture에서 안전 파일 선택 복원·충돌 파일 보존을 유지하고 구조화 preview/result event를 확인했다. TUI 누적 diff/changed-file summary와 CLI event normalization을 별도 테스트로 고정했다.
  - 검증: `$env:GOTOOLCHAIN = 'go1.26.1'; go test ./internal/agent -run '^TestUndoCheckpointSelectedRestorePreservesUnselectedConflict$' -count=1 -v` — PASS, exit 0. `go test ./cmd/proxy -run '^(TestNormalizeChatOutputPreservesStructuredUndoEvents|TestTUIAssistantSegmentsPreserveWireEventOrder|TestTUIAccumulatesRunDiffsAndListsChangedFiles)$' -count=1 -v` — PASS, exit 0. `go test ./cmd/proxy -count=1` — PASS, exit 0 (29.086s). 영향 server 회귀 `go test ./internal/server -run '^(TestSessionAPI|TestDurable|TestAgent|TestWorkstream|TestReadFile|TestFileHandlers)' -count=1` — PASS, exit 0 (6.105s). `go vet ./internal/agent ./cmd/proxy ./internal/server` — PASS, exit 0. `npm run lint`/`npm run build` — 각각 PASS, exit 0 (Vite 8.0.3; 단일 JS chunk >500 kB 경고만 유지). `git diff --check` — PASS, exit 0.
  - 당시 잔여 검사: `go test ./internal/agent -count=1`은 Chronos full-mode fixture의 16K context overflow로 exit 1이었으나 S13에서 32K fixture harness로 조정해 전체 suite가 통과했다. 이 기록은 S10 시점의 상태이며, C의 undo/diff focused 경로와 E의 browser diff/restore interaction은 당시에도 통과했다. 독립 사용자 흐름 판정은 G2에 남겼다.

- D. shared SSE parser가 chunk 경계/UTF-8/terminal/abort를 처리하도록 통일한다. project 전환 시 stale 응답이 새 화면에 쓰이지 않게 한다.

  - 초기 상태: `Chat.tsx`, `Team.tsx`, `Costs.tsx`, `lib/api.ts`가 각자 `getReader`/`TextDecoder`/줄 분할을 구현해 UTF-8이 chunk 중간에서 끊기거나 마지막 terminal frame이 남는 경우를 놓칠 수 있었고, Chat의 느린 응답이 project 전환 뒤 새 화면 상태를 갱신할 수 있었다.
  - 구현: `web/src/lib/sse.ts`의 `SSEParser`/`streamSSE`가 CRLF/LF/CR, comments, `event`, 복수 `data` line, decoder flush, terminal frame을 공통 처리한다. abort는 reader cancel과 `AbortError`로 전달한다. Chat/Team/Costs/API의 중복 parser를 이 helper로 치환했다. Chat은 send 시 workspace epoch을 캡처하고 workspace 변경 때 실행을 abort하며, 요청/stream/workstream refresh와 durable session save가 현재 project와 epoch을 확인한 뒤에만 상태를 쓴다. stale stream은 메시지·agent event·fallback session save를 기록하지 않는다.
  - 회귀: Node 22의 실제 `ReadableStream`을 임의 바이트 경계로 나눠 UTF-8 한글, CRLF/multiline data, terminal frame을 확인했다. `SSEParser` 단독 검사와 `streamSSE` 경계 검사를 모두 통과했다.
  - 검증: `npm run lint` — PASS, exit 0. `npm run build` — PASS, exit 0 (Vite 8.0.3; 단일 JS chunk >500 kB 경고만 유지). `node --experimental-strip-types --input-type=module ...` parser/stream chunk·UTF-8·terminal 검사 — PASS, exit 0. `git diff --check` — PASS, exit 0.
  - NOT RUN: 독립 사용자 흐름 판정은 G2에서 수행한다. Chromium project 전환·stale abort·모바일/키보드 상호작용은 E의 browser test에서 확인했다.

- E. [PASS] 실제 브라우저 E2E를 추가하고 모바일/키보드/테마 대비/이미지 paste를 확인한다.

  - 구현: `internal/server/ui_browser_test.go`가 설치된 Chrome/Chromium을 Rod headless로 실행하고 실제 `Server.Start`와 embedded Web bundle을 연다. 인증 bootstrap cookie, 두 registered project, refresh 후 선택 project 유지, text/binary/too-large 파일 viewer, Chat context strip, mobile viewport, keyboard input, theme toggle를 검증한다. Browser request hijack는 deterministic fake SSE만 `/api/agent`에 주입해 지연된 첫 응답을 project 전환 뒤 release하고 stale marker가 새 화면에 쓰이지 않는지, 후속 diff/undo preview가 Changes/Restore UI에 표시되고 restore 버튼이 동작하는지를 확인한다. live provider와 clipboard는 사용하지 않는다.
  - 검증: `$env:GOTOOLCHAIN = 'go1.26.1'; go test ./internal/server -run '^TestS10BrowserUIProjectFilesAndStaleStreams$' -count=1 -v` — PASS, exit 0 (Chrome 152.0.7977.77 headless, 5.99s). 전체 `go test ./internal/server -count=1` — PASS, exit 0 (37.075s). `go vet ./internal/server` — PASS, exit 0. `npm run lint`/`npm run build` — PASS, exit 0 (Vite 8.0.3; 단일 JS chunk >500 kB 경고만 유지). embedded `internal/server/webdist`는 최신 bundle로 갱신했고 `git diff --check` — PASS, exit 0.
  - NOT RUN: 실제 외부 provider, OS clipboard paste, 설치 브라우저가 없는 환경의 자동 Chromium download. 이미지 upload/ImageRead는 S09/Q11의 실제 PNG browser 검증으로 이미 확인했으며, 이 E2E는 S10 UI 경계에 집중한다.

## 완료 조건

Q04/Q06/Q10/Q11/Q12. 동일 basename 다른 경로, 느린 응답 뒤 프로젝트 전환, failed restore, 새로고침 후 작업 소속/권한 복구.

## 회귀·제약

UI 상태만 바꿔 서버 기능 연결 완료라고 처리 금지. 제품 흐름에 내부 receipt/schema 상세를 과도하게 노출하지 않는다.

## 검증 기록

A~E PASS. S10은 완료됐고 G2에서 독립 CLI/Web 사용자 흐름 검토를 진행한다. S10 시점의 Chronos full-mode fixture 기록은 S13에서 해소됐다.

### 런타임 후속 수정 — 2026-09-20

A10: 완료 없는 EOF·미완성 SSE 완료 이벤트를 실패로 처리하고 부분 응답 보존·세션 확인 안내를 추가했다. SSE 단위 검사와 실제 Chromium의 저장 불변/프로젝트 전환 검증은 [감리 보고서](../verify-report.md)의 추가 런타임 기록을 따른다.
